---
tags:
  - ML
---
Numeric and categorical features should be processed separately. Different types of preprocessing are mapped to different sets of columns via `ColumnTransformer` which in turn can be put into a `Pipeline` object.

* [[Preprocessing Numeric Features]]
* [[Encoding Categorical Variables]]

